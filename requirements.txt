git checkout main
git pull origin main

cat > requirements.txt <<'EOF'
streamlit==1.49.1
EOF

git add requirements.txt
git commit -m "Test: minimal requirements (streamlit only)"
git push origin main
